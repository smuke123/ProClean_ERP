import { Router } from 'express';

const router = Router();

// Aquí puedes agregar las rutas de usuarios cuando las necesites
// router.get('/users', getAllUsers);
// router.post('/users', createUser);

export default router;
